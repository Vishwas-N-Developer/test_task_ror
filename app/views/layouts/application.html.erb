<!DOCTYPE html>
<html>
  <head>
    <title>LoginDevise</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div id="user_nav">
    <% if user_signed_in? %>
        Signed in as <%= current_user.email %>. This cannot be cheese?
        <%#= button_to("Log Out",destroy_user_session_path,method: :delete) %>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete %> |
        <%= link_to 'Edit Account', edit_user_registration_path %><br/>

        <%= link_to 'Article', articles_path %> |
        <%= link_to 'Category', categories_path %> |
        <%= link_to "Tags", tags_path %> |
        <%= link_to 'My Article', my_articles_articles_path %> 
    <% end %>
    </div>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>" style="color: green;"><%= value %></div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
